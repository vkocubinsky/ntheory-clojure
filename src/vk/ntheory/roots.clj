(ns vk.ntheory.roots
  (:require [vk.ntheory.basic :as b]
            [vk.ntheory.validation :as v]
            [clojure.core :as c])
  )





(defn order
  "Find multiplicative order of given integer `a` in Z/Zn"
  [a n]
  
  )

